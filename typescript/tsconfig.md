## tsconfig.json 파일을 이용한 컴파일

- tsconfig.json 파일은 타입스크립트 프로젝트를 컴파일할 때 필요한 컴파일 설정 정보를 담고 있음.
- 브라우저 환경 뿐 아니라 타입스크립트의 어떠한 특징을 컴파일할지에 따른 옵션을 모두 포함

```bash
# tyescript init
$ tsc --init

message TS6071: Successfully created a tsconfig.json file.
```